<Window
    x:Class="xyz.Client.Presentation.Dialogs.TextSelectDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="输入"
    Width="400"
    Height="260"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="TextPromptText"
            FontSize="14"
            FontWeight="SemiBold" />

        <TextBox
            x:Name="InputTextBox"
            Grid.Row="1"
            Margin="0,8,0,0"
            Padding="4"
            TextChanged="OnInputTextChanged" />

        <TextBlock
            x:Name="SelectPromptText"
            Grid.Row="2"
            Margin="0,12,0,0"
            FontSize="14"
            FontWeight="SemiBold" />

        <ComboBox
            x:Name="SelectComboBox"
            Grid.Row="3"
            Margin="0,6,0,0"
            Padding="4"
            SelectionChanged="OnSelectionChanged" />

        <StackPanel
            Grid.Row="4"
            Margin="0,16,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                x:Name="OkButton"
                Width="80"
                IsDefault="True"
                IsEnabled="False"
                Click="OnOkClick"
                Content="确定" />
            <Button
                Width="80"
                Margin="8,0,0,0"
                IsCancel="True"
                Click="OnCancelClick"
                Content="取消" />
        </StackPanel>
    </Grid>
</Window>
